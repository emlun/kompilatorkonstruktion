object Factorial /* Factorial#0 */ {
  def main() : Unit = {
    println( new Fact/**/().computeFactorial/**/ ( 10 ) );
  }
}

class Fact /* Fact#1 */ {
  def computeFactorial /* computeFactorial#4 */ ( num /* num#3 */ : Int ) : Int = {
    var num_aux /* num_aux#2 */ /* num_aux#2 */ : Int;
    if ( (num/**/ < 1) )
      num_aux/**/ = 1;
    else
      num_aux/**/ = (num/**/ * this/**/.computeFactorial/**/ ( (num/**/ - 1) ));
    return num_aux/**/;
  }
}
