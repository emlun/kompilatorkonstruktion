object Main {
  def main(): Unit = {
    if(new RealMain().realMain()) {}
  }
}

class RealMain {
  def realMain(): Bool = {
    var a: Int;
    a = this.foo();
    return false;
  }
  def foo(): Int = {
    return 1;
  }
}
