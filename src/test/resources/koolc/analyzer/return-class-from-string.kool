object Main {
  def main(): Unit = {
    if(new RealMain().realMain()) {}
  }
}

class RealMain {
  def realMain(): Bool = {
    var a: String;
    a = this.foo();
    return false;
  }
  def foo(): String = {
    return new RealMain();
  }
}
