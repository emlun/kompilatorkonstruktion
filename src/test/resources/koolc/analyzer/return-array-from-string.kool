object Main {
  def main(): Unit = {
    if(new RealMain().realMain()) {}
  }
}

class RealMain {
  def realMain(): Bool = {
    var a: String;
    this.foo();
    return false;
  }
  def foo(): String = {
    return new Int[1];
  }
}
