object Main { def main(): Unit = {} }

class Main {}

class Foo {
  var bar: Bool;
  var bar: Int;

  def bar(bar: Bool, bar: Int): Bool = { return true; }
  def bar(bar: Int, bar: Bool): Int = { return 1; }
}

class Foo {}
