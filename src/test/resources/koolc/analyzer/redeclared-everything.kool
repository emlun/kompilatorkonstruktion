object Main { def main(): Unit = {} }

class Main {}

class Foo {
  var bar: Boolean;
  var bar: Int;

  def bar(bar: Boolean, bar: Int): Boolean = { return true; }
  def bar(bar: Int, bar: Boolean): Int = { return 1; }
}

class Foo {}
